fn strlen(s: str) -> u64 {
	i: u64 = 0;
	while(s[i] != 0) i++;
	return i;
}

fn write(fd: i32, s: str, len: u64) -> u64 {
        asm (fd, s, len) {
        	"mov rax, 1",
        	"mov rdi, %0",
        	"mov rsi, %1",
        	"mov rdx, %2",
        	"syscall",
        }
        ret: u64 = asmr["rax"];
        return ret;
}

fn print(s: str) -> void {
	write(1, s, strlen(s));
}

fn entry() -> i32 {
        print("test");
        return 69;
}
